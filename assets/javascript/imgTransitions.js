// create an array of image urls
var imgUrls = [
  'path/to/img1.jpg',
  'path/to/img2.jpg',
];

// create function to swap out the image url using jquery
// $el.css('backgroun-image', 'url(' + imgUrls[0] ')');

// create timer that will call the above functionality when threshold reached
